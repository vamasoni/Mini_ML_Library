# requirements.txt
# Core dependencies for Mini-ML library and testing

numpy>=1.24
pytest>=8.0

# Optional visualization or dataset helpers
graphviz>=0.20.3

# You can safely comment these out if not needed:
# matplotlib>=3.7
# pandas>=2.0
